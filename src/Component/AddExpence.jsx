import React from 'react';
import Button from './Button';

function AddExpence(props) {
    return (
        <div className="Form">
            <Button name="Add New Expence"/>
        </div>
    );
}

export default AddExpence;